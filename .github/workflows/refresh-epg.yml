name: Refresh EPG

on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch: {}

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Call refresh endpoint (EPG only)
        run: |
          curl -sS -f -X GET \
            "https://${{ secrets.VERCEL_DOMAIN }}/api/cron/refresh?target=epg&secret=${{ secrets.CRON_SECRET }}"
